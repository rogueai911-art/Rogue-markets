"use client";
import {useState} from "react";
const stocks=[["NVDA","NVIDIA","$178.32","+2.41%"],["TSLA","Tesla","$421.18","-0.84%"],["AMD","AMD","$168.77","+1.92%"],["AMC","AMC Entertainment","$2.91","+4.30%"]];
const prompts=["Why is NVDA moving today?","Analyze Tesla","Compare AMD vs NVDA","Find unusual volume"];
export default function Home(){const[q,setQ]=useState("");const[chat,setChat]=useState<string[]>([]);
const ask=(x=q)=>{if(!x.trim())return;setChat(v=>[...v,x]);setQ("")};
return <main><aside><div className="logo">R<span>O</span>GUE</div><div className="tag">MARKETS // AI TERMINAL</div><nav><b>⌂ Overview</b><b>◈ Watchlist</b><b>⌁ Screener</b><b>▣ News</b><b>◉ Paper Trade</b></nav><div className="side">AI MARKET INTELLIGENCE<br/><small>ROGUE v1.0</small></div></aside><section className="main"><header><span><i>● LIVE</i> MARKET INTELLIGENCE</span><span>BTC <strong>$112,420</strong>　 S&P 500 <strong>6,742</strong></span></header><div className="hero"><label>ROGUE AI</label><h1>What are you<br/><em>looking at today?</em></h1><p>Ask about stocks, ETFs, crypto, news, earnings and market moves.</p><div className="search"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&ask()} placeholder="Ask ROGUE anything about the market..."/><button onClick={()=>ask()}>ASK ↗</button></div><div className="prompts">{prompts.map(p=><button key={p} onClick={()=>ask(p)}>{p}</button>)}</div></div><div className="grid"><div className="panel"><div className="title">WATCHLIST <small>VIEW ALL →</small></div>{stocks.map(s=><div className="row" key={s[0]}><strong>{s[0]}</strong><span>{s[1]}</span><b>{s[2]}</b><i>{s[3]}</i></div>)}</div><div className="panel"><div className="title">ROGUE PULSE <small>MARKET SIGNALS</small></div><div className="signal"><em></em><div><b>AI analysis engine</b><p>Live data integration is the next layer. ROGUE will analyze price, volume, news and filings.</p></div></div><div className="signal"><em></em><div><b>Research mode</b><p>Ask for facts, scenarios and risk factors — not blind guesses.</p></div></div></div></div>{chat.length>0&&<div className="chat"><label>ROGUE SESSION</label>{chat.map((x,i)=><div key={i}>› {x}<small>Ready for live analysis.</small></div>)}</div>}<footer>ROGUE MARKETS <span>Research terminal • Not financial advice</span></footer></section></main>}
